# 方向 B 续轮 · 2026-09-14

沿用用户选定的「纸感阅读室」。原方向 B 已由前一轮合并到 main；本轮在 `codex/frontend-reading-followup` 继续，尚未合并或推送。

## 已修复

- 搜索：Tab 经过顶栏搜索框不再自动弹出面板，后续「新解析」按钮可达；点击、输入或提交仍打开搜索。面板内 Tab 循环，Escape 关闭后恢复入口焦点，结果导航后关闭面板。
- 可访问名称：补充搜索输入与关闭按钮标签及弹窗关联。
- 字号：日期、待整理原因、版本与差异信息等辅助文本补齐到 12px；搜索面板底部允许换行。
- 图谱：按实际截断后标签长度（含 #）预留完整宽度，避免节点圆圈直径限制文字占位。

## 自主反馈循环

逐路由截图 → 检查溢出、裁切和脚本异常 → 操作关键流程 → 肉眼复核截图 → 修复 → 重跑受影响检查。
本轮覆盖文档库、时间轴、图谱、数据看板、待整理、标签、问答、设置、导出、新解析、修复报告，并抽查编辑器与 PDF 状态。

- `followup-audit/audit.json`：11 路由 × 390/768/1440 三种宽度，33 项通过，无记录的横向溢出、控件裁切或 pageerror。
- `followup-interactions/results.json`：9 组交互通过，包括搜索键盘焦点、7 个数据视图错误恢复、图谱、编辑器、PDF 与问答。
- 相关 pytest：59 passed，7 条依赖弃用警告；本轮未重跑全量套件，前轮 881 项仅为历史记录。
- `git diff --check` 通过。

截图复核发现并修正了测试漏检：图谱清除筛选后必须等新一轮渲染且存在节点，标签宽度断言才有效。重新生成的 desktop-graph.png 已显示节点。
问答测试响应补齐真实契约中的 question 与 turn_index，并检查等待态、问题与轮次，避免测试夹具制造空白对话。

## 范围与限制

使用隔离的 populated-storage 夹具。保存、上传、模型响应等变更请求使用模拟传输；没有调用付费模型或修改用户文档。
几何与交互检查不等于所有视觉状态均已穷尽。图谱拟合时会随视口缩放；本轮检查标签预留宽度，不宣称任意缩放下字体都保持 12px。
失败态自动化覆盖 7 个数据视图的加载错误、问答失败重试和 PDF 上传失败；未声称覆盖所有业务错误。

脚本为 `scripts/frontend_audit.mjs` 与 `scripts/frontend_interactions.mjs`。复跑方式见 `docs/design/frontend-loop/README.md`。
本轮完成后停止，不留后台无限循环；后续新增问题可沿同一证据流程继续。

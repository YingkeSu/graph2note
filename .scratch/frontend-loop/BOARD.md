# 前端反馈循环 · 2026-09-12

当前阶段：方向 B「纸感阅读室」实施完成并合并主分支（merge `69b1edf`，2026-09-12）。
分支 `dev/frontend-loop-b`（基线 `c1313f0`，15 个提交）；未 push origin。

## 已完成

- 从现有 HTML、CSS、路由和视图提取页面清单。
- 写入 design-spec.md 与 brand-spec.md；制作三份真实 HTML 初稿。
- 完成 11 路由 × 2 个窗口尺寸的空库只读巡检，证据 evidence/audit.json。
- 三个原型均通过筛选选中状态和上传按钮预览提示检查；390px 无页面横向溢出；未捕获 pageerror。
- 肉眼检查三张桌面初稿和现有 390px 文档库截图。
- 方向 B 实施：暖纸底、赤陶强调色、阅读式标题、紧凑侧栏与清楚层级；逐页修改理由见 progress.md。
- 跨页回归：巡检 11 路由 × {390,768,1440} = 33 行，横向溢出 0 / 控件裁切 0 / pageerror 0；
  交互脚本 8/8 PASS；全量 pytest 881 passed。
- 修改前（空库/有内容）与修改后（有内容/空态/失败态）截图、证据与剩余限制均已归档。

## 优先待修

1. 共享顶栏：~~390px 页面宽度溢出，「新解析」挤出窗口~~ 已修（顶栏 flex 收缩链 + ≤600px 换行）；
   复核 390/768/1440 均无横向溢出，「新解析」在视口内可达。
2. 文档库：~~窄窗口标题和筛选换行为逐字竖排~~ 已修（`.library-head` 换行 + 筛选独立成行 + `white-space:nowrap`）。
3. 窄窗口侧栏：已改为紧凑布局（≤900px 默认折叠、横向换行导航、首屏高度受控）。

## 逐页状态

文档库、时间轴、图谱、数据看板、待整理、标签、问答、设置、导出、新解析、修复报告、编辑器 + PDF 详情：
均完成方向 B 优化、修改前后截图、相关测试与失败态；详见 `.scratch/frontend-loop/progress.md`。
有内容状态、网络错误状态（7 个数据视图）、编辑器/PDF 详情已覆盖；键盘全流程抽查（跳转链接 + Tab 焦点环）。

## 剩余限制（评审记录）

- 辅助字号：绝大多数辅助标签为 12px，正文 14px；``.doc-card .doc-date`` 与 ``.inbox-reason`` 仍为 11px
  （低于「辅助标签 ≥12px」1px），属非阻断残留项，可后续一行样式修正。
- 键盘：390px 下顶栏「新解析」在视觉上可达，但因 P3 统一搜索面板在搜索框获得焦点时抢占焦点，
  顺序 Tab 不会停在该按钮（此行为源自未改动的 `search-panel.js`，非本轮引入）。
- 失败态覆盖 7 个数据视图的加载错误；编辑器保存失败、PDF 上传失败沿用既有 toast/状态行。
- 自动化只证明几何、无 pageerror 与关键交互；视觉质量以截图肉眼复核为准，不宣称「设计优秀」。
- 文档编辑器无「修改前」截图（实现被发现时已存在于工作树），仅存修改后截图与测试证据。

下一步：如需继续，处理上述字号残留项与键盘焦点抢占（另行开分支）。
